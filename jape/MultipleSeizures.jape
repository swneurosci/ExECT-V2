Phase: MultipleSeizures
Input:  Lookup
Options: control=all

Rule:GetPluralSeizures
( 
{Lookup}
):tag --> 
:tag{ 

String lastChar = doc.getContent().getContent(tagAnnots.lastNode.getOffset()-1).toString();
if("s".equals(lastChar)) {
features = Factory.newFeatureMap(); features.put("plural", "yes"); outputAS.add(tagAS.firstNode(), tagAS.lastNode(), "Lookup", features); 
} 
}